---
tags:
  - Learning
source:
开始日期:
课程链接:
课时数:
进度: 0
完成日期:
任务状态:
倍速:
平台:
modified_at: 2026-04-06
---
![[noteNav]]
![[genTOC]]
# 1 Overview
> [!multi-column]
>
>> [!important|mk-green] # Quick Access
>> Open `VIEW[{课程链接}][link]`
>
>> [!summary|mk-orange] # 进度
>> ```dataviewjs
>> const current = dv.current().进度;
>> const total = dv.current().课时数;
>>
>> if (typeof current === "number" && typeof total === "number" && total > 0) {
>>     const percentage = (current / total) * 100;
>>     const blockCount = 18;
>>     const moons = ["🌑", "🌘", "🌗", "🌖", "🌕"];
>>     const totalUnits = blockCount * (moons.length - 1);
>>     const filledUnits = Math.max(0, Math.min(totalUnits, Math.round((percentage / 100) * totalUnits)));
>>     let moonBar = "";
>>     for (let i = 0; i < blockCount; i++) {
>>         const unitValue = filledUnits - i * (moons.length - 1);
>>         const moonIndex = Math.max(0, Math.min(moons.length - 1, unitValue));
>>         moonBar += moons[moonIndex];
>>     }
>>     dv.container.style.cssText = "display:flex;flex-wrap:nowrap;white-space:nowrap;";
>>     dv.container.innerHTML = `<span style="white-space:nowrap;">${moonBar} ${percentage.toFixed(2)}%</span>`;
>> } else {
>>     dv.container.innerHTML = `<span>—</span>`;
>> }
>> ```
>
>> [!info|mk-red] # 相关链接
>> -

![[courseProgress]]
# 2 笔记
````columns
id: 21342533
===

===

````
